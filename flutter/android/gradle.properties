org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# Left false (as the Flutter template set it), and paired with AGP 8 in
# settings.gradle.kts — see the note there. On AGP 9 this project's plugins
# have no working combination: false breaks file_picker, true breaks
# cloud_firestore.
android.builtInKotlin=false
