rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Default deny — all access requires explicit rules.
    // Rules are added per-feature as collections are introduced.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
